@model IEnumerable<dynamic>

@{
    ViewData["Title"] = $"Providers - {ViewBag.State}";
}

<div class="container-fluid py-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="display-5 fw-bold text-success">
            @ViewBag.State State Providers
            <span class="text-muted fs-4">(@ViewBag.TotalProviders total)</span>
        </h1>

        <a asp-action="ExportProviders" asp-route-state="@ViewBag.State"
           class="btn btn-success btn-lg shadow">
            <i class="bi bi-file-earmark-excel"></i> Export to Excel
        </a>
    </div>

    <!-- Search Form -->
    <div class="card shadow-sm mb-4">
        <div class="card-body">
            <form asp-action="Providers" method="get">
                <input type="hidden" name="state" value="@ViewBag.State" />
                <div class="row g-3">
                    <div class="col-md-8">
                        <input type="text" name="search" value="@ViewBag.Search"
                               class="form-control form-control-lg rounded-3"
                               placeholder="Search by provider name, code or location..." />
                    </div>
                    <div class="col-md-4">
                        <button type="submit" class="btn btn-success btn-lg w-100 rounded-3">Search</button>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <!-- Providers Table -->
    <div class="card shadow-lg rounded-4 overflow-hidden">
        <div class="table-responsive">
            <table class="table table-hover mb-0">
                <thead class="table-success">
                    <tr>
                        <th>Code</th>
                        <th>Provider Name</th>
                        <th>Level</th>
                        <th>Location</th>
                        <th>Phone</th>
                        <th>Email</th>
                        <th>Status</th>
                    </tr>
                </thead>
                <tbody>
                    @if (!Model.Any())
                    {
                        <tr>
                            <td colspan="7" class="text-center py-5 text-muted fs-5">
                                No providers found in this state
                            </td>
                        </tr>
                    }
                    else
                    {
                        @foreach (var p in Model)
                        {
                            <tr>
                                <td><strong class="text-primary">@p.Code</strong></td>
                                <td>@p.Name</td>
                                <td>@p.Level</td>
                                <td>@p.Location</td>
                                <td>@p.Phone</td>
                                <td>@p.Email</td>
                                <td>
                                    <span class="badge @(p.IsActive ? "bg-success" : "bg-danger") px-4 py-2">
                                        @(p.IsActive ? "Active" : "Inactive")
                                    </span>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>

    <!-- PAGINATION UI -->
    @if (ViewBag.TotalPages > 1)
    {
        <nav class="mt-5 d-flex justify-content-center">
            <ul class="pagination pagination-lg shadow-sm">

                <!-- Previous -->
                <li class="page-item @(ViewBag.CurrentPage == 1 ? "disabled" : "")">
                    <a class="page-link rounded-start-3"
                       asp-action="Providers"
                       asp-route-state="@ViewBag.State"
                       asp-route-search="@ViewBag.Search"
                       asp-route-page="@(ViewBag.CurrentPage - 1)">
                        ← Previous
                    </a>
                </li>

                <!-- Page Numbers -->
                @for (int i = 1; i <= ViewBag.TotalPages; i++)
                {
                    <li class="page-item @(i == ViewBag.CurrentPage ? "active" : "")">
                        <a class="page-link"
                           asp-action="Providers"
                           asp-route-state="@ViewBag.State"
                           asp-route-search="@ViewBag.Search"
                           asp-route-page="@i">
                            @i
                        </a>
                    </li>
                }

                <!-- Next -->
                <li class="page-item @(ViewBag.CurrentPage == ViewBag.TotalPages ? "disabled" : "")">
                    <a class="page-link rounded-end-3"
                       asp-action="Providers"
                       asp-route-state="@ViewBag.State"
                       asp-route-search="@ViewBag.Search"
                       asp-route-page="@(ViewBag.CurrentPage + 1)">
                        Next →
                    </a>
                </li>
            </ul>
        </nav>
    }

    <div class="text-center text-muted mt-3">
        Showing page @ViewBag.CurrentPage of @ViewBag.TotalPages
    </div>
</div>